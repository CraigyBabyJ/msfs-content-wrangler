<Window
    x:Class="MSFS.ContentWrangler.App.ContentXmlPickerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Choose Content.xml"
    SizeToContent="WidthAndHeight"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select a Content.xml file:" Margin="0,0,0,8" />
        <ComboBox x:Name="PickerBox" Grid.Row="1" MinWidth="640" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="90" IsDefault="True" Click="OnOkClick" Margin="0,0,6,0" />
            <Button Content="Cancel" Width="90" IsCancel="True" Click="OnCancelClick" />
        </StackPanel>
    </Grid>
</Window>
