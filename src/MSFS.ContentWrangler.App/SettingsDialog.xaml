<Window
    x:Class="MSFS.ContentWrangler.App.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Settings"
    Width="820"
    Height="560"
    MinWidth="700"
    MinHeight="480"
    WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl x:Name="SettingsTabs" Grid.Row="0">
            <TabItem Header="Categorization Rules">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Categories" Style="{StaticResource HeaderTextStyle}" />
                    <ListBox Grid.Row="1" Grid.Column="0" x:Name="CategoryList" DisplayMemberPath="Name" SelectionChanged="OnCategorySelected" />
                    <WrapPanel Grid.Row="2" Grid.Column="0" Margin="0,8,0,0">
                        <Button Content="Add..." Click="OnAddCategory" Margin="0,0,6,0" />
                        <Button Content="Remove" Click="OnRemoveCategory" Margin="0,0,6,0" />
                        <Button Content="Move Up" Click="OnMoveCategoryUp" Margin="0,0,6,0" />
                        <Button Content="Move Down" Click="OnMoveCategoryDown" />
                    </WrapPanel>

                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="PatternLabel" Text="Patterns" Margin="8,0,0,0" Style="{StaticResource HeaderTextStyle}" />
                    <ListBox Grid.Row="1" Grid.Column="1" x:Name="PatternList" Margin="8,0,0,0" />

                    <WrapPanel Grid.Row="2" Grid.Column="1" Margin="8,8,0,0">
                        <TextBox x:Name="PatternInput" Width="240" ToolTip="Enter new pattern" Margin="0,0,6,0" />
                        <Button Content="Add" Click="OnAddPattern" Margin="0,0,6,0" />
                        <Button Content="Remove Selected Pattern" Click="OnRemovePattern" />
                    </WrapPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Paths">
                <StackPanel Margin="12">
                    <TextBlock Text="Content.xml Path:" Style="{StaticResource HeaderTextStyle}" />
                    <WrapPanel Margin="0,6,0,0">
                        <TextBox x:Name="ContentPathInput" Width="520" Margin="0,0,6,0" />
                        <Button Content="Browse..." Click="OnBrowseContentXml" />
                    </WrapPanel>
                </StackPanel>
            </TabItem>

            <TabItem Header="Appearance">
                <StackPanel Margin="12">
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Theme" Style="{StaticResource HeaderTextStyle}" />
                        <RadioButton x:Name="ThemeDark" Content="Dark Theme" GroupName="Theme" />
                        <RadioButton x:Name="ThemeLight" Content="Light Theme" GroupName="Theme" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Display" Style="{StaticResource HeaderTextStyle}" />
                        <CheckBox x:Name="ShowThumbnailsCheck" Content="Show package thumbnails in list" />
                        <CheckBox x:Name="CleanLegacyCheck" Content="Clean legacy FS20 mods" />
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Maintenance" Style="{StaticResource HeaderTextStyle}" />
                        <Button Content="Clear thumbnail cache" Click="OnClearThumbCache" />
                    </StackPanel>
                </StackPanel>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK"
                    Width="90"
                    IsDefault="True"
                    Click="OnOkClick"
                    Margin="0,0,6,0"
                    Style="{StaticResource SecondaryActionButton}" />
            <Button Content="Cancel"
                    Width="90"
                    IsCancel="True"
                    Click="OnCancelClick"
                    Style="{StaticResource SecondaryActionButton}" />
        </StackPanel>
    </Grid>
</Window>
